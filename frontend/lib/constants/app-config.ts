// src/lib/constants/app-config.ts

export const APP_CONFIG = {
  APP_NAME: 'Capital Ranker',
  DEFAULT_API_TIMEOUT: 15000, // milliseconds
  PAGINATION_LIMIT: 10,
  TOKEN_REFRESH_INTERVAL: 1000 * 60 * 10, // 10 minutes
  DEBOUNCE_TIME: 500, // milliseconds for debounce hooks
  MAX_FILE_UPLOAD_SIZE_MB: 5,
  // Add other global configurations as needed
};
